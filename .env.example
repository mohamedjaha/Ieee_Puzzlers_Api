# Example environment variables - DO NOT COMMIT real secrets to the repository
# Copy this file to a local .env and fill in real secrets on your machine or CI/CD system.

# SQL Server (used by the sqlserver service)
SA_PASSWORD=

# Database account for the application (recommended: non-sa user)
DB_USER=
DB_PASSWORD=

# JWT secret (keep this private, rotate regularly)
JWT_SECRET=

# Optional: set host if connecting to a host SQL server
DB_HOST=sqlserver

# Example override for ASPNETCORE environment
ASPNETCORE_ENVIRONMENT=Production
